fn test(a: i32): i32 {
    mut inc: i32 = 0
    while (a) : (inc++) {
        a--
    }
    return inc
}

fn go(a: i32): i32 {
    loop: if (a == 10) {
        a = 5
        continue :loop
    }
    return a
}

fn tests(a: i32): i32 {
    end: while {
        break :end
    }
    return a
}